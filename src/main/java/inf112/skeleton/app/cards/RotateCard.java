package inf112.skeleton.app.cards;

public class RotateCard extends Card {

    public RotateCard(int distance, int priority, Direction direction) {
        super(distance, priority, direction);
    }
}
